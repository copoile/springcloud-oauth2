spring:
  security:
    user:
      name: admin
      password: admin
  application:
    name: eureka-server
server:
  port: 8000

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://admin:admin@localhost:${server.port}/eureka/